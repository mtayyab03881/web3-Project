// Store the quote and its author
const prophetQuote: string = "The best among you are those who have the best manners and character.";
const author: string = "Prophet Muhammad (peace be upon him)";

// Create the output message
const output: string = `"${prophetQuote}" - ${author}`;

// Print the output
console.log(output);
