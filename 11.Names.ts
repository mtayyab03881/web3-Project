// Store the names of your friends in an array
const names: string[] = ["Ali", "Umer", "Uzair", "Umair"];

// Print each person's name one at a time
console.log("My Friends' Names:");

// Access and print each name using a loop
for (let i = 0; i < names.length; i++) {
  console.log(names[i]);
}
